<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ninelives" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest if needed %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag asset_path("icon.png"), rel: "icon", type: "image/png" %>
    <%= favicon_link_tag asset_path("icon.svg"), rel: "icon", type: "image/svg+xml" %>
    <%= apple_touch_icon_link_tag asset_path("icon.png") %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>

